app = "clean-housekeeper-bot"
primary_region = "ams"

[build]
  dockerfile = "Dockerfile"

[env]
  PYTHONUNBUFFERED = "1"

# ❗️ГОВОРИМ FLY: ЭТО WORKER, НЕ WEB
[processes]
  worker = "python main.py"

# ❗️ОТКЛЮЧАЕМ HTTP ПОЛНОСТЬЮ
[services]
